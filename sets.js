/*
    Sets doesn't allow duplicate values -> unordered collection of unique values
    We can track something like users IP address

    sadd ip 1 -> adds new member inside set
    srem -> removes member from set
    sismember -> checks if member exists in set -> returns in true or false
*/
